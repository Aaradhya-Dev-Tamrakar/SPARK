# Firmware Engineer Task Briefing (Pre-Hardware Phase)

**Assignee:** Rupesh Kadel (Roll 79034 / BEI)  
**Subsystem:** Embedded Systems, ESP-IDF Firmware, Layer 1 Gate & On-Chip Inference  
**Primary Directory:** [`firmware/`](file:///f:/Aaradhya-Dev-Tamrakar/SPARK/firmware/)  
**Goal:** Deliver 100% verified, host- and emulator-tested firmware before physical ESP32-S3 boards arrive.

---

## 1. Why You Don't Need Physical Silicon Yet
Professional embedded engineers do not wait for printed circuit boards to write firmware. Using **QEMU for ESP32-S3** and the **Wokwi Simulator**, you can simulate the ESP32-S3 dual-core Xtensa LX7 CPU, allocate the FreeRTOS heap, run the TFLite Micro interpreter, and feed synthetic 200 Hz IMU data via simulated I2C registers.

---

## 2. Rupesh's Action Checklist

### Task 1: Run the Native Host Unit Tests
* **Objective:** Ensure all existing C++ algorithmic units pass natively on your machine without any microcontroller connected.
* **Location:** `firmware/test/`
* **Execution:**
  ```bash
  cd firmware/test
  # Compile with native gcc/clang or CMake
  mkdir -p build && cd build
  cmake ..
  cmake --build .
  ctest --output-on-failure
  ```
* **Acceptance Criteria:** 21/21 unit tests passing (Layer 1 gate threshold checks, circular buffer indexing, resultant acceleration calculation).

---

### Task 2: Setup ESP32-S3 Virtual Simulation (Wokwi or QEMU)
* **Objective:** Boot ESP-IDF firmware on an emulated ESP32-S3 target.
* **Option A: VS Code Wokwi Extension (Recommended)**
  1. Install the "Wokwi Simulator" extension in VS Code.
  2. Create a `diagram.json` in `firmware/` with an `esp32-s3-devkitc-1` and an `mpu6050`.
  3. Wire I2C pins: SDA to GPIO 21, SCL to GPIO 22.
  4. Build firmware via ESP-IDF: `idf.py build`
  5. Press `F1` $\to$ `Wokwi: Start Simulator`.
* **Option B: ESP-IDF QEMU**
  ```bash
  cd firmware
  idf.py set-target esp32s3
  idf.py build
  idf.py qemu monitor
  ```
* **Acceptance Criteria:** Firmware boots to `app_main()`, initializes FreeRTOS tasks, and logs startup telemetry over virtual UART.

---

### Task 3: Profile the INT8 TFLite Micro Model in RAM
* **Objective:** Verify that `spark_cnn_int8.h` ($18.5\text{ KB}$ model) loads into the tensor arena without overflowing SRAM.
* **File to Inspect:** [`firmware/main/models/spark_cnn_int8.h`](file:///f:/Aaradhya-Dev-Tamrakar/SPARK/firmware/main/models/spark_cnn_int8.h)
* **Implementation Steps:**
  1. Define a dedicated tensor arena:
     ```cpp
     constexpr int kTensorArenaSize = 35 * 1024; // 35 KB budget
     static uint8_t tensor_arena[kTensorArenaSize];
     ```
  2. Initialize the `tflite::MicroInterpreter` with `spark_cnn_model`.
  3. Call `AllocateTensors()` and log exact bytes used:
     ```cpp
     ESP_LOGI("SPARK_ML", "Tensor Arena Used: %d / %d bytes", 
              interpreter->arena_used_bytes(), kTensorArenaSize);
     ```
  4. Pass a dummy $200 \times 6$ normalized INT8 window to verify `interpreter->Invoke()` succeeds.
* **Acceptance Criteria:** TFLite Micro executes virtual inference and logs execution time in microseconds.

---

### Task 4: Validate Wire Format Output Serialization
* **Objective:** Ensure the JSON payload generated by the firmware matches [`docs/WIRE_FORMAT_v1.md`](file:///f:/Aaradhya-Dev-Tamrakar/SPARK/docs/WIRE_FORMAT_v1.md).
* **Verify:**
  ```json
  {
    "event_id": "EVT-1001",
    "device_id": "SPARK-NODE-01",
    "firmware_version": "1.0.0",
    "timestamp_ms": 128540,
    "confidence": 0.892,
    "peak_features": {
      "a_x": 0.42, "a_y": 2.81, "a_z": 1.15,
      "w_x": 120.4, "w_y": 340.2, "w_z": 45.1
    }
  }
  ```
* **Acceptance Criteria:** Output JSON prints cleanly to UART monitor upon simulated Layer 1 + Layer 2 trigger.

---

## 3. Defense Questions Rupesh Must Be Prepared to Answer

**Q1: "Why did you choose an ESP32-S3 over standard ESP32 or STM32?"**  
* **Answer:** *"The ESP32-S3 includes Xtensa PIE (Processor Instruction Extensions) vector instructions, enabling accelerated DSP and INT8 matrix multiplication for TensorFlow Lite Micro. It also provides integrated Bluetooth 5 (LE) on a single sub-$5 SoC without requiring external coprocessors."*

**Q2: "How does your Layer 1 threshold gate prevent the MCU from constantly draining battery?"**  
* **Answer:** *"Layer 1 is implemented as a lightweight arithmetic calculation in a 200 Hz hardware timer ISR. It calculates resultant acceleration $|a| = \sqrt{a_x^2 + a_y^2 + a_z^2}$ in under $0.05\text{ ms}$. Heavy CNN inference (Layer 2) is only triggered if $|a| > 2.5g$ for $>300\text{ ms}$, saving up to 80% battery power during normal daily living."*

**Q3: "How did you verify your firmware before the boards arrived?"**  
* **Answer:** *"We used QEMU and Wokwi virtual target environments with host-executable C++ unit tests. We validated memory allocation for the 35 KB tensor arena, simulated 200 Hz I2C register bursts, and verified serialization against our locked wire format schema."*
